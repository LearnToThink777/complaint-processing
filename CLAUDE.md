# 작업 지침

## 기능 작업 완료 후 프로젝트 재시작

기능 개선/추가 작업을 완료한 뒤, 사용자에게 대화 입력을 넘기기 전에 실행 중인
프로젝트 서버를 **직접(서브태스크로 위임하지 말고) 재시작**한다. 사용자가 바로
확인할 수 있는 상태로 만들어 두는 것이 목적이다.

- 백엔드(FastAPI): 기존에 떠 있던 `uvicorn` 프로세스를 종료 후
  `uvicorn --app-dir .. complaint_processing.api:app --reload` 로 재기동한다.
  (`--reload` 플래그가 있으므로 코드 변경은 보통 자동 반영되지만, 변경 반영이 안 되거나
  프로세스가 죽어 있으면 반드시 수동으로 재시작한다.)
- 프론트엔드(`frontend/`, Vite): dev 서버를 띄워 작업 중이었다면 동일하게 재시작한다.
- 재시작 후 서버가 정상 기동했는지(에러 로그 없음, 응답 확인) 확인하고 나서 사용자에게
  결과를 보고한다.
